# VisionMeter
This project is a vision screening web app. It displays letters that get smaller at each level. The user identifies the letters to test visual clarity. It evaluates each eye separately and generates a final report.
